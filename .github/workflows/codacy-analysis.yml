name: Codacy Analysis

on:
  pull_request:
    branches:
      - main

jobs:
  codacy-analysis:
    name: Codacy Analysis
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run Codacy analysis
        uses: codacy/codacy-analysis-action@4.0.4
        with:
          project-token: ${{ secrets.CODACY_PROJECT_TOKEN }}
